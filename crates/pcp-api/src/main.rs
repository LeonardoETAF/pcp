//! Servidor HTTP do PCP (Axum): autenticação, autorização deny-by-default e endpoints
//! sob `/pcp/...`. Raiz de composição do backend. (CLAUDE.md §2/§7)
#![forbid(unsafe_code)]
#![warn(clippy::all, clippy::pedantic)]

fn main() {
    // Esqueleto: a auth entra no prompt 0.4 e os endpoints no 2.1.
}
